<%= form_for(type_section) do |f| %>
  <% if type_section.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(type_section.errors.count, "error") %> prohibited this type_section from being saved:</h2>

      <ul>
      <% type_section.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :active %>
    <%= f.check_box :active %>
  </div>

  <div class="field">
    <%= f.label :abrev %>
    <%= f.text_field :abrev %>
  </div>

  <div class="field">
    <%= f.label :tag %>
    <%= f.text_field :tag %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
